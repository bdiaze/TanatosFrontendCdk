import { FormatoCorreo } from './formato-correo';

describe('FormatoCorreo', () => {
  it('should create an instance', () => {
    const directive = new FormatoCorreo();
    expect(directive).toBeTruthy();
  });
});
