import { BgImageFadeIn } from './bg-image-fade-in';

describe('BgImageFadeIn', () => {
  it('should create an instance', () => {
    const directive = new BgImageFadeIn();
    expect(directive).toBeTruthy();
  });
});
