import { FormatearTelefonoPipe } from './formatear-telefono-pipe';

describe('FormatearTelefonoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearTelefonoPipe();
    expect(pipe).toBeTruthy();
  });
});
